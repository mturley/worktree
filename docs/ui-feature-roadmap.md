# Worktree UI feature roadmap

A running list of UI enhancements for `worktree ui` — things we want but have
deferred, with enough context to pick each one up later. Add to this as ideas
come up; move items to "Done" (or delete) when shipped.

Cleared 2026-08-25 after the 2026-08 run of UI work shipped. Everything the
previous version recorded is in git history (`git log -- docs/ui-feature-roadmap.md`)
and, where it still constrains the code, in `docs/web-ui-architecture.md`.

## In progress

_Nothing in progress._

## Deferred

_Nothing deferred._
